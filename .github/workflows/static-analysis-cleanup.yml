name: Temporary static analysis cleanup

on:
  push:
    branches: [dev]
    paths:
      - '.github/workflows/static-analysis-cleanup.yml'

permissions:
  contents: write

concurrency:
  group: temporary-static-analysis-cleanup
  cancel-in-progress: true

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: gd, intl, mbstring, pdo_pgsql, zip
          coverage: none

      - name: Install PHP dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Apply Pint formatting
        run: vendor/bin/pint

      - name: Report PHPStan
        run: composer analyse || true

      - name: Commit formatting changes
        run: |
          set -euo pipefail
          if git diff --quiet -- app; then
            echo 'No app formatting changes.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app
          git commit -m 'style: normalize PHP formatting'
          git push origin HEAD:dev
